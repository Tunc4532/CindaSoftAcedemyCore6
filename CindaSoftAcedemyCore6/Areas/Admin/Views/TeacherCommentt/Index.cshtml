@model List<CommentTeacher>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Yorumlar </h4>
            <p class="card-description">
                Düzenleme <code>.Öğretmen Yorumları</code>
            </p>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th> # </th>
                            <th> Fotoğraf</th>
                            <th> İsim </th>
                            <th> Şehir </th>
                            <th> Sil </th>
                            <th> Güncelle </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.CommentTeacherID</td>
                                <td class="py-1">
                                    <img src="@item.ImageUrl" alt="image" />
                                </td>
                                <td> @item.Name </td>
                                <td> @item.City </td>
                                <td><a href="/Admin/TeacherCommentt/DeleteTeacherComment/@item.CommentTeacherID" class="btn btn-outline-danger">Sil</a></td>
                                <td><a href="/Admin/TeacherCommentt/EditTeacherComment/@item.CommentTeacherID" class="btn btn-outline-warning">Güncelle</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a href="/Admin/TeacherCommentt/AddTeacherComment/" class="btn btn-outline-info">Yeni Ekle</a>
            </div>
        </div>
    </div>
</div>

